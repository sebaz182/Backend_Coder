<div>
    <h2>Carritooo</h2>
    <div class="contenedor">
        
        <ul id="cart">
            {{#each cart.products}}
            <li>
                {{product.title}} , {{product.category}} $<strong>
                    ${{product.price}} </strong> | Llevas {{quantity}}
            </li>
            <hr>
            {{/each}}
        </ul>
        <br><br>

    </div>
</div>