<div class="container d-flex justify-content-center align-items-center">
    <div class="col-md-6">
        <br><br>
        <h2 class="text-center">Login</h2>
        <hr>
        <form onsubmit="login(event)" id="formLogin" action="/api/sessions/login" method="post">
            {{#if error}}
                <div class="alert alert-danger fw-bold">{{error}}</div>
            {{/if}}
            {{#if mensaje}}
                <div class="alert alert-info fw-bold">{{mensaje}}</div>
            {{/if}}
            <div class="mb-3">
                <label for="email" class="form-label fw-bold me-2">Correo Electrónico</label>
                <input type="email" class="form-control" id="email" name="email" placeholder="Correo Electrónico">
            </div>
            <div class="mb-3">
                <label for="password" class="form-label fw-bold me-2">Contraseña</label>
                <input type="password" class="form-control" id="password" name="password" placeholder="Contraseña">
            </div>
            <input type="hidden" name="web" value="web">
            <div class="text-center">
                <input type="submit" class="btn btn-primary" value="Login">
            </div>
        </form>
        <br>
        <div class="text-center">
            <a class="btn btn-success" href="/api/sessions/github">Acceder con Github</a>
        </div>
    </div>
</div>

<script src="/assets/js/login.js"></script>